image: ubuntu

build: off

build_script:
  - pwd && ls -la
  - which ruby && ruby --version
  - rvm use 2.4.1 && which ruby && ruby --version
  - rvm use 2.4.1 && ./build_gem.sh

artifacts:
    - path: '**\metabox\*.gem'